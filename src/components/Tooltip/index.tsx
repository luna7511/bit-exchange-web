import {styled} from "@mui/material/styles";
import MuiTooltip, {tooltipClasses, type TooltipProps} from "@mui/material/Tooltip";

const Tooltip = styled(MuiTooltip)(({ theme }) => ({

}));

export {
    Tooltip
}